import 'package:flutter/material.dart';

class AppColors {
  // Main Theme
  static const Color primary = Color(0xFF1B1B1B); // Dark buttons and text
  static const Color secondary = Color(0xFFFFC107); // Yellow used in pills

  // Backgrounds
  static const Color backgroundLight = Color(0xFFE9F1F7); // Soft light blue
  static const Color backgroundWarm = Color(
    0xFFFFF6D9,
  ); // Warm yellow for gradients
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF1E212D);
  static const Color textSecondary = Color(0xFF757575);

  // States
  static const Color divider = Color(0xFFE0E0E0);
  static const Color error = Color(0xFFE53935);
  static const Color success = Color(0xFF10B981); // Emerald 500
}
